using System;
using System.Collections.Generic;
using System.Linq;

namespace Models
{
    public class @Model.Name
    {
    @foreach (var column in @Model.Columns)
    {
        @:@Include("_Property.cshtml", column, typeof(DatabaseSchemaReader.DataSchema.DatabaseColumn))
    }
    }
}
